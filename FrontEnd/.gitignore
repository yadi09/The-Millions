# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr

# Canonical lockfile here is pnpm-lock.yaml. The Docker build uses `npm install`
# (not `npm ci`), so an npm-generated package-lock.json isn't needed and would
# just create noise. Ignore it.
package-lock.json

# Environment files
.env
.env.local
.env.*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
