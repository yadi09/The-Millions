# Vite production build. Same-origin design: nginx terminates TLS, serves
# the static SPA on /, and proxies /api/* to the backend container. So the
# frontend just calls `/api/...` (relative) — no CORS, no preflights.
VITE_API_BASE_URL=/api
